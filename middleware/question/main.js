const isLoggedIn = require(`./isLoggedIn`);
const isVerified = require(`./isVerified`);
const checkCurrent = require(`./checkCurrent`);

module.exports.isLoggedIn = isLoggedIn;
module.exports.isVerified = isVerified;
module.exports.checkCurrent = checkCurrent;
